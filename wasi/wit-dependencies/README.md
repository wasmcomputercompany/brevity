# WIT Dependencies

This module contains a single task, `downloadWitFiles`, which downloads WIT file
dependencies from OCI, the WASM component registry, using
[wkg](https://github.com/bytecodealliance/wasm-pkg-tools), the WASM packaging
tool.
